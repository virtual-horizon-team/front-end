import CartPageClient from "./cart-client";

export const metadata = {
  title: "Shopping Cart | Virtual Horizon Marketplace",
  description: "Review your cart and proceed to checkout to purchase assets."
};

export default function CartPage() {
  return <CartPageClient />;
}
